/**
 * Client-safe featured-pin types and resolvers (no `firebase-admin` / Node builtins).
 * Server reads: `getSiteFeatured` from `@/lib/site-featured-server`.
 */
export * from "@/lib/site-featured-core";
